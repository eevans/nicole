
  * Some urls are still absolute (style refs, feed links, etc)
  * Something better is needed for login/logout.
  * Decouple page title and header (make configurable).
  * Need the ability to add abritrary content to the sidebar.
  * Display count next to tag/category.

  x The Atom feeds seems broken, doesn't display article.
